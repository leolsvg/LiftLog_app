name: liftlog
description: Une app de muscu et nutrition.
publish_to: 'none'

version: 1.0.1+2

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.6.0
  percent_indicator: ^4.2.5
  fl_chart: ^0.70.0
  shared_preferences: ^2.2.2
  firebase_core: ^3.1.1
  firebase_auth: ^5.1.1
  cloud_firestore: ^5.0.2
  mobile_scanner: ^7.2.0
  google_sign_in: ^6.2.1
  sign_in_with_apple: ^6.0.0
  font_awesome_flutter: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# CETTE SECTION DOIT ÊTRE À LA RACINE (DÉCALÉE VERS LA GAUCHE)
flutter:
  uses-material-design: true